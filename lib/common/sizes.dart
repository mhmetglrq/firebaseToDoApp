import 'package:flutter/widgets.dart';

const scaffoldPadding = EdgeInsets.all(15);
//All
const allPadding40 = EdgeInsets.all(40);
const allPadding20 = EdgeInsets.all(20);
const allPadding15 = EdgeInsets.all(15);
const allPadding10 = EdgeInsets.all(10);
const allPadding8 = EdgeInsets.all(8);
const allPadding5 = EdgeInsets.all(5);

//Vertical
const vertical10 = EdgeInsets.symmetric(vertical: 10);
const vertical20 = EdgeInsets.symmetric(vertical: 20);
const vertical30 = EdgeInsets.symmetric(vertical: 30);
const vertical40 = EdgeInsets.symmetric(vertical: 40);
const vertical5 = EdgeInsets.symmetric(vertical: 5);
//Horizontal
const horizontal40 = EdgeInsets.symmetric(horizontal: 40.0);
const horizontal35 = EdgeInsets.symmetric(horizontal: 35.0);

const horizontal30 = EdgeInsets.symmetric(horizontal: 30.0);
const horizontal20 = EdgeInsets.symmetric(horizontal: 20.0);
const horizontal15 = EdgeInsets.symmetric(horizontal: 15.0);

const horizontal10 = EdgeInsets.symmetric(horizontal: 10.0);
const horizontal5 = EdgeInsets.symmetric(horizontal: 5);

//onlyTop
const top5 = EdgeInsets.only(top: 5);

//onlyLeft
const left5 = EdgeInsets.only(left: 5);
const left15 = EdgeInsets.only(left: 15);

//onlyBottom
const bottom30 = EdgeInsets.only(bottom: 30);
